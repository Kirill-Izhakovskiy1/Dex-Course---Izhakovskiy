let string3 = "Строка"

// Также существуют обратные кавычки которые позволяют всталять выражение в строку. 
let backQuates1 = `Здесь вставляется выражение ${Objeckt1.name} `

let a = 35;
let b = 11;
console.log(`Fifteen is ${a + b} and not ${2 * a + b}.`); 
// Иначе пришлось бы использовать круглые скобочки. в которые оборачиваются выражения и каждый рах использовать плюс "+" при соединении выражения с строкой.
